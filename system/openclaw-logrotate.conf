/var/log/openclaw/*.log /var/log/openclaw/skills/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0640 root root
    postrotate
        systemctl reload openclaw > /dev/null 2>&1 || true
    endscript
}
